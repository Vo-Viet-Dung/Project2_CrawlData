import React from 'react';
import PropTypes from 'prop-types';

Ribbon.propTypes = {
    
};

function Ribbon(props) {
    return (
        <div>
            
        </div>
    );
}

export default Ribbon;